import React from 'react';
import GameBoard from '../lib/components/GameBoard.js';

const Play = () => {
  return (
    <main className="page home">
      <GameBoard />
    </main>
  );
};

export default Play;
